.next
node_modules
pnpm-lock.yaml

# Generated from docs/api/openapi.yaml by `pnpm gen:types`. Prettier would
# reformat it, and then `pnpm gen:types:check` could never pass again.
src/lib/types/api.ts

# Generated by `msw init public/` (the `msw.workerDirectory` field in
# package.json regenerates it). Formatting it would only be undone next time.
public/mockServiceWorker.js
