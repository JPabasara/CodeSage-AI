# Copy this file to secrets.prod.env when deploying.
# Fill secrets.prod.env locally or on the server.
# Never commit secrets.prod.env.

# Data stores
CODESAGE_DATABASE_URL=
CODESAGE_MIGRATION_DATABASE_URL=
CODESAGE_REDIS_URL=

# KEDA Redis scaler settings.
# For Upstash, split rediss://default:PASSWORD@HOST:6379 into:
#   address  -> HOST:6379
#   username -> default
#   password -> PASSWORD
CODESAGE_KEDA_REDIS_ADDRESS=
CODESAGE_KEDA_REDIS_USERNAME=default
CODESAGE_KEDA_REDIS_PASSWORD=

# App signing and authentication
CODESAGE_SECRET_KEY=
CODESAGE_ASGARDEO_BASE_URL=
CODESAGE_ASGARDEO_CLIENT_ID=
CODESAGE_ASGARDEO_CLIENT_SECRET=
CODESAGE_ASGARDEO_REDIRECT_URI=https://api.codesageai.dev/api/auth/callback

# Invitation email (Resend)
# Sending-only API key from https://resend.com/api-keys. For production, verify
# your domain and replace onboarding@resend.dev with an address on it.
CODESAGE_RESEND_API_KEY=
CODESAGE_INVITATION_FROM_EMAIL=CodeSage <onboarding@resend.dev>

# Browser and session boundaries
CODESAGE_FRONTEND_BASE_URL=https://codesageai.dev
CODESAGE_CORS_ORIGINS=["https://codesageai.dev"]
CODESAGE_COOKIE_DOMAIN=.codesageai.dev
CODESAGE_COOKIE_SECURE=true

# Internal services
CODESAGE_ML_SERVICE_URL=http://ml:8001
CODESAGE_ML_TIMEOUT_SECONDS=30

# Optional integrations
# Optional read-only token for public GitHub repository metadata and branches.
CODESAGE_GITHUB_TOKEN=
